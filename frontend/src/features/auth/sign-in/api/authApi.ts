// API functions for authentication
export const signInApi = async (email: string, password: string) => {
  // This would typically make an HTTP request to your backend
  // For now, we'll use the existing auth context
  throw new Error('Use the auth context signIn method instead');
};

export const signOutApi = async () => {
  // This would typically make an HTTP request to your backend
  // For now, we'll use the existing auth context
  throw new Error('Use the auth context signOut method instead');
};
